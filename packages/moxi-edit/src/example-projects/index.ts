import { project as basicPixiOnly } from './basic-pixi-only';
import { project as basicReactPixi } from './basic-moxi';
import { project as moxiBehavior } from './moxi-behavior';
import { project as moxiProgressBar } from './progress-bar';

export const examplesLibrary = [
  basicPixiOnly,
  basicReactPixi,
  moxiBehavior,
  moxiProgressBar
];
