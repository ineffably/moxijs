{
  "name": "moxi",
  "version": "0.1.0",
  "description": "",
  "private": true,
  "workspaces": [
    "packages/moxijs",
    "packages/moxijs-examples",
    "packages/pikcell"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "build:clean": "npm run build:clean --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present",
    "build:dev": "npm run build:dev --workspaces --if-present",
    "watch": "concurrently \"npm run watch --workspace=moxijs-examples\" \"npm run watch --workspace=moxijs\"",
    "watch:moxi": "npm run watch --workspace=moxijs",
    "examples": "npm run local --workspace=moxijs-examples",
    "test": "npm run test --workspace=moxijs",
    "test:watch": "npm run test:watch --workspace=moxijs",
    "test:ci": "npm run test:ci --workspace=moxijs",
    "pikcell": "npm run dev --workspace=pikcell"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "concurrently": "^9.2.1",
    "eslint": "^9.39.1",
    "eslint-plugin-react": "^7.37.5",
    "rimraf": "^6.1.0",
    "typescript-eslint": "^8.46.3"
  }
}
